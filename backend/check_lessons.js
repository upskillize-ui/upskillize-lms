const {Sequelize} = require('sequelize'); const s = new Sequelize('upskillize_lms', 'root', 'Puja', {host:'localhost', dialect:'mysql', port:3306}); s.query('SELECT id, course_name FROM courses LIMIT 5').then(([r])=>{ console.log('COURSES:', JSON.stringify(r,null,2)); return s.query('SELECT id, module_name, course_id FROM course_modules LIMIT 5'); }).then(([r])=>{ console.log('MODULES:', JSON.stringify(r,null,2)); process.exit(0); }).catch(e=>{ console.error(e.message); process.exit(1); });
